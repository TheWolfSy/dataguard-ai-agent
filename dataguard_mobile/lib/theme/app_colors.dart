import 'package:flutter/material.dart';

class DGColors {
  DGColors._();

  // Brand
  static const blue = Color(0xFF60A5FA);
  static const accent = Color(0xFFF97316);
  static const accentHover = Color(0xFFFB923C);
  static const accentDark = Color(0xFFEA580C);

  // Surfaces (Dark)
  static const surfaceDark = Color(0xFF0A1630);
  static const surfaceDarkAlpha = Color(0xD10A1630);
  static const surfaceDarker = Color(0xFF060D1F);
  static const surfaceCard = Color(0xBF0A142D);
  static const borderBlue = Color(0x2E60A5FA);
  static const borderBlueStrong = Color(0x3360A5FA);

  // Surfaces (Light)
  static const surfaceLight = Color(0xFFF1F5F9);
  static const surfaceLightCard = Color(0xD9FFFFFF);
  static const borderLightBlue = Color(0x332563EB);

  // Text (Dark)
  static const textDark = Color(0xFFE2E8F0);
  static const textMutedDark = Color(0xFF64748B);
  static const textBlueDark = Color(0xFF93C5FD);

  // Text (Light)
  static const textLight = Color(0xFF1E293B);
  static const textMutedLight = Color(0xFF64748B);

  // Semantic
  static const success = Color(0xFF34D399);
  static const warning = Color(0xFFFBBF24);
  static const error = Color(0xFFF87171);
  static const critical = Color(0xFFEF4444);

  // Sidebar
  static const sidebarActive = Color(0xFFFB923C);
  static const sidebarBgDark = Color(0xEB060D1F);
  static const sidebarBgLight = Color(0xEBFFFFFF);

  // Badge
  static const badgeError = Color(0xFFFCA5A5);
  static const badgeWarning = Color(0xFFFDE68A);
  static const badgeSuccess = Color(0xFF6EE7B7);
  static const badgeInfo = Color(0xFF93C5FD);

  // Glass effect
  static const glassBorder = Color(0x2E60A5FA);
  static const glassHighlight = Color(0x0FFFFFFF);
}
